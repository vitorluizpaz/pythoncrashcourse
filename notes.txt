Variable names can contain only letters, numbers, and underscores. 
They can start with a letter or an underscore, but not with a number. 
For instance, you can call a variable message_1 but not 1_message.

.title()
.upper()
.lower()
\t
\n
.rstrip()
.lstrip()
.sort(reverse=True or False) -- Permanent change
sorted(reverse=True or False) temporary change
reverse -- Permanent
min, max, sum

if pizza in pizzas
if pizza not in pizzas

Looping through the keys is actually the default behavior when looping 
through a dictionary, so this code would have exactly the same output if you 
wrote . . . 
for name in favorite_languages